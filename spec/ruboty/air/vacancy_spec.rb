require 'spec_helper'

describe Ruboty::Air::Vacancy do
  it 'has a version number' do
    expect(Ruboty::Air::Vacancy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
